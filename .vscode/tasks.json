{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Start Analytics Backend",
			"type": "shell",
			"command": "cd python_backend && PYTHONPATH=. ./venv/bin/python -c \"import sys; sys.path.insert(0, '.'); from main import app; import uvicorn; uvicorn.run(app, host='0.0.0.0', port=8000)\"",
			"isBackground": true,
			"problemMatcher": []
		}
	]
}