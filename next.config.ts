import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  //* insert the ip address from the npm run dev
  allowedDevOrigins: ['198.168.50.242']
};

export default nextConfig;
